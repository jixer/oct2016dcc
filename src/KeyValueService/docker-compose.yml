version: '2'

services:
  keyvalueservice:
    image: jixer/oct2016dcc
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    links: 
      - db
  db:
    image: mongo