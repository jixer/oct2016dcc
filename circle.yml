general:
    build_dir: src
machine:
    pre:
        - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    services:
        - docker
dependencies:
    pre:
        - sudo pip install docker-compose
    post:
        - ./dockerTask.sh compose release
notify:
    webhooks:
        - url: https://registry.hub.docker.com/u/jixer/oct2016dcc/trigger/f19f4743-2531-44c2-900d-689bde0c40a0/